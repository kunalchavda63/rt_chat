enum FontFamily { poppins }

enum ImageType { asset, network, file }

enum FabType { normal, small, large, extended }

enum AnimationType {
  fade,
  scale,
  fadeScale,
  slideFromTop,
  slideFromBottom,
  slideFromLeft,
  slideFromRight,
  none,
}
